// 修改vant中组件变量  比如 sliderBarHeight 会转换成 `--van-slider-bar-height`
export const themeVars = {
  // buttonPrimaryBorderColor: '#128bed',
  // buttonPrimaryBackgroundColor: '#128bed',
  // black: '#000',
  // blue: '#000'
};
